<h1>New Ussd loan product</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.ussd_loan_product_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.ussd_loan_product_path(@conn, :index) %></span>
